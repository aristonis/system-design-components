# system-design-components

A place where I design backend components (system design) before building them —
requirements, UML diagrams, architecture, and pseudocode.

Design only — no runnable code. Each component will live in its own folder.
